import { Calculator} from "./calculator";
// This component is used as the main form for the page Calculator. 
// This makes it easier to expand the site in the future

export const Main = () => {

    return (
        <div>
            <div>
                <Calculator />
            </div>
        </div>
    );
};